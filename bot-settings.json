
{
  "$schema": "./bot-settings.schema.json",

  "rules": [
    {
      "name": "restream",
      "comment": "Restream Relay Mode",
      "active": true,
      "regex_name": "(?<=\\[Twitch:\\s).*?(?=\\] )",
      "regex_message": "(?<=\\]).*$"
    }
  ]
}